.env

# Root tooling (npm run verify) — no root node deps
node_modules/

# Django SQLite (local; or when DATABASE_URL unset)
db.sqlite3
*.sqlite3

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.pyc

# Host-specific (optional)
.vercel

# Local snapshot / reference tree (nested repo, may contain secrets)
OldCodes/
