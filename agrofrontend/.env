# Empty = same-origin /api on the Vite dev server → proxied to Django :8000 (see vite.config.js).
# Set only if you intentionally call the API host directly (then ensure Django is up + CORS).
VITE_API_BASE_URL=
