export default function Result({ t }) {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <p className="text-green-600 text-2xl font-semibold">{t.app_name}</p>
    </div>
  )
}
